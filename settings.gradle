include "main-instance"		// this includes the shadow instance, which should be a copy of the main instance
include "app-orchestrator" 	// this is the reliability module
include "load-balancer"
rootProject.name = 'cloudcomputing'
